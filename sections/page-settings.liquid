<div id="page-settings" class="contents">
  <style>
      :root {
        --text-color: {{ section.settings.textColor }};
        --background-color: {{ section.settings.backgroundColor }};
        --footer-text-color: {{ section.settings.footerTextColor }};
        --footer-background-color: {{ section.settings.footerBackgroundColor }};

        {% if section.settings.headerTheme == 'light' %}
          --header-theme: #F4EED0;
        {% else %}
          --header-theme: #643600;
        {% endif %}
      }
      

      body, .body-bg, #utility-nav {
        color: var(--text-color) !important;
        background-color: var(--background-color) !important;
      }

      footer > *, footer div p, footer div li , footer .logo-bg, .footer-bg {
        color: var(--footer-text-color) !important;
        background-color: var(--footer-background-color) !important;
      }     

      {% if template == 'page.hours-info' or template == 'page.faq' or template == 'page.shipping' or template == 'page.legal' or template == 'page.privacy' %}
        .nav-gradient::before {
          background: linear-gradient(180deg, var(--background-color) 0%, rgba(2, 43, 38, 0) 100%);
        }
      {% endif %}
  </style>

  {% if template == 'page.hours-info' %}
      <div class="absolute top-0 left-0 w-full bottom-0 pointer-events-none nav-gradient z-20"></div>
  {% endif %}
  {% if template == 'page.faq' or template == 'page.shipping' or template == 'page.legal' or template == 'page.privacy' %}
  <div class="absolute top-[-120px] lg:top-[-160px] left-0 w-full bottom-0 pointer-events-none nav-gradient z-20"></div>
{% endif %}

</div>

{% schema %}
{
  "name": "Page Settings",
  "class": "contents",
  "settings": [
    {
      "type": "select",
      "id": "headerTheme",
      "label": "Initial Page Theme",
      "options": [
        { "label": "Light", "value": "light" },
        { "label": "Dark", "value": "dark" }
      ],
      "default": "dark"
    },
    {
      "type": "color",
      "id": "textColor",
      "label": "Text Color",
      "default": "#643600"
    },
    {
      "type": "color",
      "id": "backgroundColor",
      "label": "Background Color",
      "default": "#FAF8EC"
    },
    {
      "type": "color",
      "id": "footerTextColor",
      "label": "Footer Text Color",
      "default": "#FAF8EC"
    },
    {
      "type": "color",
      "id": "footerBackgroundColor",
      "label": "Footer Background Color",
      "default": "#643600"
    }
  ],
  "presets": [
    {
      "name": "Page Settings"
    }
  ]
}
{% endschema %}